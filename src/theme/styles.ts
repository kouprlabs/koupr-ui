import { variables } from '../variables'

const styles = {
  global: {
    body: {
      fontFamily:
        "'Work Sans', 'PT Sans', Almarai, 'Noto Sans JP', 'Noto Sans TC', 'Noto Sans SC', 'Noto Sans KR', Poppins, 'Noto Sans Bengali'",
      fontSize: variables.bodyFontSize,
    },
  },
}

export default styles
